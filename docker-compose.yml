version: '3.8'

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: blather
      POSTGRES_PASSWORD: blather-dev
      POSTGRES_DB: blather
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
